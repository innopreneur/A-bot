module.exports = {
    uniswap : {
        sellEthBaseUrl : `http://${process.env.HOST}:${process.env.PORT}/sell/eth`,
        sellTokenBaseUrl : `http://${process.env.HOST}:${process.env.PORT}/sell/token`
    }
}